doctype html
head
    title Gradebook Accesssing Page
    style
        include ../public/stylesheets/loginStyle.css
h1 Gradebook Accesssing Page
form
  fieldset(name='logininfo')
    legend My Gradebooks
    .textboxHolder
      label(for='email') Email:
      input#email(type='text' name='email' required='' style='width:75%;font-size:14pt;' onkeyup='checkInput()')
      br
      span.hiddenHint
        p(style='padding-left:5px;padding-right:5px') Must be an email address with @cs.hku.hk or @connect.hku.hk
    .buttonHolder
      input.button(type='button' name='login' value='Login' style='width: 5em;  height: 2em;font-size:12pt;')
.hiddenMsg
if errorMsg == 'Fail to authenticate - incorrect secret'
    .errorMsg(style='background-color:#FF7575;border:2px solid red;display:block') 
        p Fail to authenticate - incorrect secret!
else if errorMsg == 'Fail to authenticate - token expired'
    .errorMsg(style='background-color:#FF7575;border:2px solid red;display:block') 
        p Fail to authenticate - OTP expired!
else if errorMsg == 'Fail to authenticate - unknow user'
    .errorMsg(style='background-color:#FF7575;border:2px solid red;display:block') 
        p Unknown user - cannot identify the student.
else if errorMsg == 'Session expired. Please login again.'
    .errorMsg(style='background-color:#FF7575;border:2px solid red;display:block') 
        p Session expired. Please login again.
script(src='javascripts/login.js')
